# macOS
.DS_Store

# Secrets — never commit
.mcp.json
.env
.env.*
!.env.example
*.key
*.pem

# Claude Code local state (repo no longer tracks anything under .claude/)
.claude/

# Data dumps (ad-hoc analysis artifacts, never in main repo)
*.tweets.json
followin_io_*.json
*.dump.json
*.db
*.sqlite
*.sqlite3

# IDE
.vscode/
.idea/
*.swp
*.swo

# SDD scratch (agent briefs/reports/diffs — 本地过程产物，不入库)
.superpowers/

# 对内文档（缺陷工单等）——本地保留，不进公开仓
docs/MCP-缺陷工单-*.md
docs/*-给Dev-*.md
